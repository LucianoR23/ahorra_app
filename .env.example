NEXT_PUBLIC_API_URL=
# Web Push (VAPID) — misma clave pública que el backend.
# Generá el par con: go run ./cmd/vapidgen (en el repo del backend)
# Pegá aquí la PUBLIC key; la PRIVATE queda solo en el backend.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
